# UI config
ui.baseUrl=https://www.saucedemo.com
ui.browser=chrome
ui.implicitWait=10
ui.pageLoadTimeout=30

# Grid config
grid.enabled=true
grid.url=http://localhost:4444
grid.maxSessions=5
grid.defaultBrowser=chrome

# Parallel execution
parallel.mode=tests
parallel.threadCount=4

# API config
api.baseUrl=https://reqres.in
api.timeout=5000
api.key=reqres-free-v1

# Credentials
ui.username=standard_user
ui.password=secret_sauce
